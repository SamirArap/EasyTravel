<% @page_title = 'Delete Pages'%>

<div class="container">
	<div class="jumbotron">
		<h2>Are you sure?</h2>
		
		<%= form_for(:page, :url => {:action => 'destroy', :id => @category}) do |f| %>
			
			<p>You want delete page <strong><%= @category.name %></strong>? </p>

			<div class="row">
				<div class="col-md-6"><%= submit_tag("Delete", :class => "btn btn-default btn-block")%></div>
				<div class="col-md-6"><%= link_to("Back", {:controller => 'category', :action => 'index'}, :class => "btn btn-default btn-block" )%></div>
			</div>
		<%end%>
	</div>
</div>
